services:
  - type: web
    name: facetiktok-bot
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
